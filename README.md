Don't change the css, it's the framework for the elements. Copy parts in index.html to alter the sight.

We need to change the map image btw

Idea btw, we could have different coloured lines to differentiate between characters

Also the sites function for git is bad so it takes a bit to update the site and it doesn't tell you when it ends up happening
